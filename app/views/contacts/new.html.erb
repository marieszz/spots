<div class="form">
  <div class="contact-intro">
      <div class="my-4">
        <h1>Une idée, une reco ou juste nous envoyer un mot doux ?</h1>
      </div>
      <div class="my-4">
        <h3>C'est ici ! </h3>
      </div>
      <div class="my-4">
        <h5>On s'engage à te répondre dans les prochains jours (entre 2 dates tu comprends).</h5>
      </div>
  </div>
  <div class="contact-form">
    <%= simple_form_for @contact do |f| %>
      <div class="form-grid">
        <div class="input-small">
          <%= f.label :name, "Ton prénom" %>
          <%= f.text_field :name, placeholder: "Marie, Myriam, Lucas,...", class: 'input-small' %>
        </div>
        <div class="input-small">
          <%= f.label :email, "Ton adresse e-mail" %>
          <%= f.email_field :email, placeholder: "lola@gmail.com", class: 'col input-small', autocomplete:  "email"%>
        </div>
      </div>
      <div class="email-message">
        <%= f.label :message, "Dis-nous tout" %>
        <%= f.text_field :message, class:"email-message" %>
      </div>
      <div class="d-flex justify-content-center">
        <%= f.button :submit, "Envoyé 💌!", class:"send-email" %>
      </div>
    <% end %>
  </div>
</div>
